# Interview Pilot AI · 工作台文案与功能规范

## 设计目标

面试中，用户的注意力应放在回答内容，而不是寻找按钮。界面只突出三件事：**正在听什么、现在该说什么、下一步怎么操作**。

视觉采用深海蓝半透明玻璃与青蓝高亮；黄色只表示录音或实时监听。正文基准为 19px，保证用户在边看视频会议边阅读时仍然清晰。

## 信息结构

```text
顶栏：品牌 / 透明度 / 置顶 / 关闭
左侧操作轨：录音、自动监听、复制答案、清空对话
主阅读区：当前问题、实时回答
底部：文字提问输入、生成按钮、运行状态
```

## 界面文案

| 区域 | 文案 | 说明 |
| --- | --- | --- |
| 品牌 | `INTERVIEW PILOT AI` | 固定品牌名。 |
| 透明度 | `透明度 88%` | 可调范围 55%–100%，数值随滑块实时变化。 |
| 录音 | `录音 / F2` | 手动录制 5 秒音频并生成回答。 |
| 自动监听（关闭） | `自动监听 / F3` | 提示可开启持续监听。 |
| 自动监听（开启） | `自动监听 / 已开启` | 检测到完整提问后自动生成回答。 |
| 复制 | `复制答案` | 复制最新一条 AI 回答。 |
| 清空 | `清空对话 / F4` | 清除本次会话显示内容。 |
| 主区标签 | `当前问题`、`实时回答` | 让用户立即识别正在看的内容。 |
| 输入提示 | `输入问题，按 Enter 生成回答…` | 支持不经语音的文字提问。 |
| 提交按钮 | `生成回答` | 与输入框和回车键的动作一致。 |

## 状态文案

| 状态 | 显示文案 | 颜色/反馈 |
| --- | --- | --- |
| 就绪 | `就绪 · F2 录音 / F3 连续监听 / F4 清空` | 中性蓝灰。 |
| 录音 | `正在录音 · 请说话（5 秒）` | 黄色状态点呼吸。 |
| 识别 | `检测到提问，正在转写…` | 青色状态点。 |
| 生成 | `正在生成回答…` | 青色状态点与省略号动画。 |
| 自动监听 | `自动监听已开启 · 检测到提问将自动生成回答` | 绿色状态点呼吸。 |
| 已复制 | `已复制最新回答` | 短暂成功反馈。 |
| 空回答 | `没有检测到有效语音，请再试一次` | 清晰说明下一步。 |
| 失败 | `转写失败：{原因}` 或 `生成失败：{原因}` | 保留具体原因，便于排查。 |

## 交互规则

1. **录音（F2）**：点击后立即显示录音状态；录制结束依次转写、生成、流式写入“实时回答”。忙碌期间再次触发不重复启动任务。
2. **自动监听（F3）**：开启后持续监听；检测到一段完整语音时自动执行转写和生成。再次点击或按 F3 关闭。
3. **文字提问**：输入内容后按 Enter 或点击“生成回答”，与语音提问使用同一套回答流。
4. **复制答案**：始终复制最新 AI 回答；没有回答时提示“还没有回答可复制”。
5. **清空对话（F4）**：清空当前问题与答案显示，不影响透明度、语言、模型和监听设置。
6. **透明度**：滑块实时应用于整个悬浮窗，并保存到 `config.json`，下次打开维持上次选择。
7. **置顶**：图钉按钮切换窗口是否始终置顶，并给出状态反馈。

## 可访问性与可读性

- 正文至少 19px；控制按钮至少 15px；状态文字至少 12px。
- 鼠标悬停、键盘焦点和启用状态均有可见边框或底色变化。
- 高频动作均有快捷键，并在按钮上直接展示。
- 不使用颜色作为唯一状态信号：状态文字和按钮文案同步变化。
